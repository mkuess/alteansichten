1. Project context

This project is called “Alte Ansichten”.
It is a Laravel 11 and Filament 3 project for a historical archive platform.

The first MVP starts with the region structure:

- countries
- states
- districts
- municipalities

The countries table already exists and contains Austria.

This task only creates the second region table: states.

Replit is only the development environment.
The project must remain standalone and deployable later on my own Linux VM at spaceship.com.

2. Current state

Laravel 11 is installed and working.
Filament 3 is installed and available at /admin.
A local admin user exists.
Simple global roles exist:

- admin
- editor
- user

The countries table exists.
The Country model exists.
CountrySeeder exists and seeds Austria idempotently.
There is exactly one country record:

- name: Austria
- slug: austria
- iso_code: AT
- status: published

No states, districts, municipalities, categories, places, or Filament Resources exist yet.

3. Exact task

Create the states database table, the State model, and a StateSeeder for the 9 Austrian federal states.

4. What Replit should do

- Create a migration for a states table.
- Create an app/Models/State.php model.
- Add the relationship:
  - State belongs to Country
  - Country has many States
- Create a database/seeders/StateSeeder.php file.
- Seed the 9 Austrian federal states.
- Connect every state to the existing Austria country record.
- Make the seeder idempotent, so running it multiple times does not create duplicates.
- Add StateSeeder to DatabaseSeeder after CountrySeeder.
- Run the migration.
- Run the seeder.
- Verify that the states table contains exactly 9 records after seeding.

5. What Replit must NOT do

- Do not create districts yet.
- Do not create municipalities yet.
- Do not create categories yet.
- Do not create places yet.
- Do not create Filament Resources yet.
- Do not create public pages yet.
- Do not add regional permissions.
- Do not install extra packages.
- Do not modify the existing role system.
- Do not continue with Task 007 or later tasks.

6. Expected files / changes

Expected files:

- database/migrations/xxxx_xx_xx_xxxxxx_create_states_table.php
- app/Models/State.php
- app/Models/Country.php updated with states relationship
- database/seeders/StateSeeder.php
- database/seeders/DatabaseSeeder.php updated to call StateSeeder after CountrySeeder

Expected states table fields:

- id
- country_id foreign key referencing countries.id
- name
- slug
- code nullable, max length 10
- status string, default published
- timestamps

Expected seeded records:

- Burgenland
- Kärnten
- Niederösterreich
- Oberösterreich
- Salzburg
- Steiermark
- Tirol
- Vorarlberg
- Wien

Use lowercase URL-friendly slugs, for example:

- kaernten
- niederoesterreich
- oberoesterreich

The model should allow mass assignment for the relevant fields.

7. Test steps

Run:

php artisan migrate

Run:

php artisan db:seed --class=StateSeeder

Run the seeder a second time:

php artisan db:seed --class=StateSeeder

Expected:
There are still exactly 9 state records.

Run:

php artisan tinker

Then check:

App\Models\State::count();

Expected:
9

Check:

App\Models\Country::where('iso_code', 'AT')->first()->states()->count();

Expected:
9

Check one example:

App\Models\State::where('name', 'Kärnten')->first()->slug;

Expected:
kaernten

Finally run:

php artisan test

Expected:
All existing tests pass.

8. Stop instruction

Please stop afterward and do not implement any further steps without being asked.