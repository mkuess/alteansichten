1. Project context

This project is called “Alte Ansichten”.
It is a Laravel 11 and Filament 3 project for a historical archive platform.

The first MVP starts with the region structure:

- countries
- states
- districts
- municipalities

The countries, states, and districts tables already exist.

This task only creates the fourth region table: municipalities.

Replit is only the development environment.
The project must remain standalone and deployable later on my own Linux VM at spaceship.com.

2. Current state

Laravel 11 is installed and working.
Filament 3 is installed and available at /admin.
A local admin user exists.
Simple global roles exist:

- admin
- editor
- user

The countries table exists and contains Austria.
The states table exists and contains the 9 Austrian federal states.
The districts table exists and contains an initial practical set of 11 districts.

Relationships already exist:

- Country has many States
- State belongs to Country
- State has many Districts
- District belongs to State

No municipalities, categories, places, or Filament Resources exist yet.

3. Exact task

Create the municipalities database table and the Municipality model.

Do not create a MunicipalitySeeder yet in this task.

4. What Replit should do

- Create a migration for a municipalities table.
- Create an app/Models/Municipality.php model.
- Add the relationship:
  - Municipality belongs to District
  - District has many Municipalities
- Use SoftDeletes for municipalities.
- Add fields needed for future public municipality pages.
- Keep the model simple and Laravel-standard.
- Run the migration.
- Verify that the municipalities table exists.

5. What Replit must NOT do

- Do not create a MunicipalitySeeder yet.
- Do not seed municipalities yet.
- Do not create categories yet.
- Do not create places yet.
- Do not create Filament Resources yet.
- Do not create public pages yet.
- Do not add media, submissions, reports, QR codes, people, or organizations.
- Do not add regional permissions.
- Do not install extra packages.
- Do not modify the existing role system.
- Do not continue with Task 009 or later tasks.

6. Expected files / changes

Expected files:

- database/migrations/xxxx_xx_xx_xxxxxx_create_municipalities_table.php
- app/Models/Municipality.php
- app/Models/District.php updated with municipalities relationship

Expected municipalities table fields:

- id
- district_id nullable foreign key referencing districts.id
- name
- slug
- summary nullable
- description nullable long text
- hero_image_path nullable
- logo_path nullable
- postal_code nullable, max length 20
- latitude nullable decimal, precision suitable for coordinates
- longitude nullable decimal, precision suitable for coordinates
- status string, default draft
- public_profile_enabled boolean, default true
- internal_reference_code nullable, max length 50
- timestamps
- soft deletes

The model should use SoftDeletes and allow mass assignment for the relevant fields.

7. Test steps

Run:

php artisan migrate

Expected:
create_municipalities_table runs successfully.

Run:

php artisan tinker

Then check:

Schema::hasTable('municipalities');

Expected:
true

Check:

Schema::hasColumn('municipalities', 'district_id');

Expected:
true

Check:

Schema::hasColumn('municipalities', 'deleted_at');

Expected:
true

Finally run:

php artisan test

Expected:
All existing tests pass.

8. Stop instruction

Please stop afterward and do not implement any further steps without being asked.