1. Project context

This project is called “Alte Ansichten”.
It is a Laravel 11 and Filament 3 project for a historical archive platform.

The first MVP needs categories for places / locations.
Categories will later be used by places and in the public frontend for filtering.

This task only creates the categories table, Category model, and CategorySeeder.

Replit is only the development environment.
The project must remain standalone and deployable later on my own Linux VM at spaceship.com.

2. Current state

Laravel 11 is installed and working.
Filament 3 is installed and available at /admin.
A local admin user exists.
Simple global roles exist:

- admin
- editor
- user

The following region tables and models already exist:

- countries
- states
- districts
- municipalities

The municipalities table exists but has no seeder yet.
No categories, places, media, submissions, reports, QR codes, or Filament Resources exist yet.

3. Exact task

Create the categories database table, the Category model, and a CategorySeeder with the first MVP place categories.

4. What Replit should do

- Create a migration for a categories table.
- Create an app/Models/Category.php model.
- Create a database/seeders/CategorySeeder.php file.
- Seed the initial 12 place categories.
- Make the seeder idempotent, so running it multiple times does not create duplicates.
- Add CategorySeeder to DatabaseSeeder after the region seeders.
- Run the migration.
- Run the seeder.
- Verify that the categories table contains exactly 12 records after seeding.

5. What Replit must NOT do

- Do not create places yet.
- Do not create media tables yet.
- Do not create Filament Resources yet.
- Do not create public pages yet.
- Do not create municipality seeders yet.
- Do not add QR code functionality yet.
- Do not add submissions, reports, people, or organizations.
- Do not install extra packages.
- Do not modify the existing role system.
- Do not continue with Task 010 or later tasks.

6. Expected files / changes

Expected files:

- database/migrations/xxxx_xx_xx_xxxxxx_create_categories_table.php
- app/Models/Category.php
- database/seeders/CategorySeeder.php
- database/seeders/DatabaseSeeder.php updated to call CategorySeeder

Expected categories table fields:

- id
- name
- slug
- description nullable
- sort_order unsigned integer, default 0
- status string, default published
- timestamps

Expected seeded records:

- Gasthaus
- Schule
- Bahnhof
- Sakralbau
- Handwerk
- Industriebau
- Denkmal
- Wohngebäude
- Verwaltung
- Verkehr
- Landschaft
- Sonstiges

Use lowercase URL-friendly slugs, for example:

- gasthaus
- sakralbau
- wohngebaeude

The model should allow mass assignment for the relevant fields.

7. Test steps

Run:

php artisan migrate

Run:

php artisan db:seed --class=CategorySeeder

Run the seeder a second time:

php artisan db:seed --class=CategorySeeder

Expected:
There are still exactly 12 category records.

Run:

php artisan tinker

Then check:

App\Models\Category::count();

Expected:
12

Check one example:

App\Models\Category::where('name', 'Wohngebäude')->first()->slug;

Expected:
wohngebaeude

Check:

App\Models\Category::where('name', 'Gasthaus')->first()->status;

Expected:
published

Finally run:

php artisan test

Expected:
All existing tests pass.

8. Stop instruction

Please stop afterward and do not implement any further steps without being asked.